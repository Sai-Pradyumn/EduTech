# Frontend configuration.
# Angular reads config from src/environments/environment.ts — these values
# document what to set there (or wire as build-time replacements for prod).
API_BASE_URL=http://localhost:3000/api
SOCKET_URL=http://localhost:3000
